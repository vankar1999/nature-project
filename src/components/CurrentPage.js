//come back to this later, we want to separate out the page rendering from the navbar
import React from 'react'

export default function CurrentPage({currentPage}) {
  return (
    <div>{currentPage}</div>
  );
}
